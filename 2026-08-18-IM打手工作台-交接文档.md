# 星竞电竞 · 站内对话（IM）与打手工作台 — 交接文档

> 日期：2026-08-18
> 承接：上一轮《联调交接报告-2026-08-18.md》已完成后端缺陷修复 + 小程序/admin 对接真实后端。本份文档记录**本轮新增功能（站内对话 + 打手工作台，后端已完成并验证）**，供下一个 AI 接手继续开发前端。
> 本份文档**自包含**，面向零上下文接手者；如需既有系统细节，先读 `联调交接报告-2026-08-18.md` 与两份设计/计划文档（见 §10）。

---

## 1. 项目全景

| 子项目 | 目录 | 技术栈 | 当前状态 |
|--------|------|--------|----------|
| 后端 | `xingjing-server/` | NestJS 11 + TypeORM(MySQL) + Redis + JWT + socket.io | ✅ 运行中（watch 模式 `localhost:3000/api`） |
| 小程序 | `三角洲陪玩小程序/` | 微信小程序原生 | ⏳ 顾客端已连真实后端；**对话页 / 打手工作台未开发** |
| 管理后台 | `admin/` | Vite + Vue3 + Pinia | ✅ dev 运行（`localhost:5173`）；**敏感词/派单/绑定码 UI 未开发** |
| 设计文档 | `docs/superpowers/` | Markdown | spec + plan 已定稿 |

**业务**：龙岩市新罗区星竞电竞俱乐部有限公司的微信陪玩小程序，自营模式。本次目标：**顾客与打手在平台内站内对话，禁止私下加微信/留联系方式**；打手端提供完整工作台（待接单池 / 接单 / 服务中 / 完成）。

## 2. 本轮新增功能（后端已完成 ✅）

1. **身份体系**：微信用户可通过「绑定码」关联到打手记录（`boosters.user_id`），登录响应带 `boosterInfo`；新增 `BoosterGuard`（校验 `role==='booster'` + 打手 `audit==='approved'`）。
2. **订单状态机扩展**：支付后进入 `pending_assign`（待接单池）再被接单；三种分配模式 `grab 抢单 / assign 派单 / select 指定打手`。
3. **打手工作台接口**：待接单池（按分配模式过滤可见性）、接单（接单即开始服务）、完成、在线/接单开关。
4. **敏感词过滤**：词库 + 硬规则（手机号/微信号/QQ 号），管理端 CRUD。
5. **站内对话（HTTP）**：会话/消息两表，发起会话、历史分页、发送（敏感词命中整条拒绝）、已读。
6. **WebSocket 实时推送**：socket.io `/chat` namespace，JWT 握手，`chat:new / chat:unread / chat:read`。
7. **管理端扩展**：`POST /admin/orders/:id/assign` 派单、`GET /admin/boosters/:id/bind-code` 绑定码、服务 `assignMode` 配置。

## 3. 架构与关键约定

- 统一响应 `{ code, data, msg }`（code 0 成功）；金额一律「分」；**时间戳一律毫秒**，bigint 列 TypeORM 返回字符串 → VO 里 `toTs()`/`Number()` 转换，前端 `new Date(ts)` 直接用。
- 全局 `JwtAuthGuard`（`@Public()` 放行）；管理员 `AdminGuard`；打手 `BoosterGuard`。
- DTO 一律 class-validator + 全局 `ValidationPipe(whitelist, transform, stopAtFirstError)`。
- 订单状态流转用**事务 + 悲观锁**（`SELECT ... FOR UPDATE`），防并发重复流转/重复接单。
- 依赖注入注意：`ChatService` 注入 `ChatGateway` 实现 WS 推送；`OrdersService` 注入 `SettingsService`、`Booster` repo；`BoostersService` 注入 `Order` repo（计数）。
- 模块依赖：`OrdersModule ← BoostersModule`（工作台用 OrdersService）；`ChatModule` imports `UsersModule` + `SensitiveModule`；均无循环依赖。

## 4. 数据库变更（synchronize:true 自动建表）

**新增列**
- `orders`：`booster_id int nullable`（接单打手记录 id）、`assign_mode varchar(16) default 'grab'`、`pending_assign_at bigint nullable`、`cancel_reason varchar(255) default ''`（上轮已有）
- `services`：`assign_mode varchar(16) default ''`（空 = 用全局默认）
- `users`：`role varchar(16) default 'user'`（user / booster）
- `boosters`：`user_id int nullable`，`@Index({ unique: true })`
- `settings`：`assignMode`（默认 `grab`，写入 `AppSettings` 接口 + `DEFAULTS`）

**新增表**
- `conversations`：`order_id`（唯一索引）、`user_id`、`booster_user_id`、`booster_id`、`last_message`、`last_message_at bigint`、`user_unread`、`booster_unread`、`status`
- `messages`：`conversation_id`（索引）、`sender_id`、`sender_role`、`content`、`msg_type`（text，预留 image/voice）
- `sensitive_words`：`word`、`type`（word / regex）

## 5. 新增接口清单（后端）

### 身份与绑定
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/booster/bind` | 绑定码关联打手 `{ code }` → `{ boosterInfo }`（仅登录态） |
| GET | `/api/booster/me` | 打手本人信息 + 待接单数（BoosterGuard） |
| POST | `/api/booster/me/status` | 更新在线/接单开关 `{ online?, accepting? }` |

### 打手工作台（全部 BoosterGuard）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/booster/orders?status=` | `pending_assign` 即待接单池；其余状态 = 我的订单 |
| GET | `/api/booster/orders/:id` | 订单详情（打手） |
| POST | `/api/booster/orders/:id/accept` | 接单（接单即开始服务，事务+悲观锁防并发） |
| POST | `/api/booster/orders/:id/complete` | 打手确认完成 |

### 对话（顾客/打手共用，登录态）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/chat/conversations` | 会话列表（对端信息/最后消息/未读） |
| POST | `/api/chat/conversations` | 发起会话 `{ orderId }`（须已分配打手，已存在复用） |
| GET | `/api/chat/conversations/:id/messages?beforeId=&limit=` | 历史消息（向下翻页，默认 20 上限 50） |
| POST | `/api/chat/conversations/:id/messages` | 发消息 `{ content }`（敏感词命中 400） |
| POST | `/api/chat/conversations/:id/read` | 标记已读 |

### 顾客端补充
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/orders/:id/booster` | 订单关联打手（未接单返回 null） |

### 管理端（AdminGuard）
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/admin/orders/:id/assign` | 指派打手 `{ boosterId }` |
| GET | `/api/admin/boosters/:id/bind-code` | 生成一次性绑定码（15 分钟有效，Redis `bind:code:<code>`） |
| GET/POST/DELETE | `/api/admin/sensitive-words` | 敏感词 CRUD |
| PUT | `/api/admin/services/:id` | 支持 `assignMode`（`grab/assign/select/''`） |

### WebSocket
- 连接：socket.io → `http://localhost:3000/chat`，握手 `auth.token` 或 `query.token`（JWT），校验失败断开。
- 事件：`chat:new`（双方 + 发送者其他端）、`chat:unread`（对端）、`chat:read`（对端已读回执）。
- 连接追踪为内存 `Map<userId, Set<socketId>>`，多端可共存；未读数在服务端维护（`user_unread`/`booster_unread`）。

## 6. 订单状态机（扩展后）

```
pending_pay ──支付──► paid ──自动──► pending_assign(待接单) ──接单──► in_progress ──完成──► completed
     │ 取消               │                                        │
     ▼                    └── 取消                                 └── 退款
 cancelled                                                       refunding ─► refunded（驳回回 refundFrom）
```

**退款可发起状态**：`pending_assign / in_progress / completed`（含 `paid` 兼容，见 `REFUNDABLE_STATUSES`）。

**三种分配模式**（`orders.assign_mode`，下单时按 `服务配置 || settings.assignMode || 'grab'` 落库）：
| 模式 | 待接单池可见性 | 接单入口 |
|---|---|---|
| grab | 所有在线且接单中的打手可见 | 打手主动 accept |
| assign | 仅被 admin 指派打手可见 | 打手 accept 确认 |
| select | 仅下单时指定打手可见 | 打手 accept 确认 |

## 7. 运行与验证

### 环境（本地联调）
- MySQL `127.0.0.1:3307`（root / `xingjing123`，库 `xingjing`）；Redis `127.0.0.1:6380`（无密码）；均由 `xingjing-server/docker-compose.local.yml` 拉起。**本机 3306/6379 原生服务勿动**。
- 后端 `localhost:3000/api`（`npm run start:dev` watch 模式，改代码自动重载）；admin dev `localhost:5173`。
- `.env`：`WECHAT_MOCK=true`、`PAY_MOCK=true`、`ADMIN_PASSWORD=xingjing-admin-123`（admin 登录用）、强随机 `JWT_SECRET`。
- 微信 mock 登录：`openid = mock_ + md5(code)`，同一 `code` 登录即同一用户。

### 编译
```bash
cd xingjing-server && npm run build   # 必须全绿，无 TS 错误
```

### E2E 脚本（`xingjing-server/scripts/`，前置：服务运行 + seed 已执行 + 打手 1「夜风」为 approved）
| 脚本 | 验证内容 | 运行 |
|---|---|---|
| `e2e-workbench.ts` | 绑定闭环 + 下单支付→待接单→接单→完成 + 重复接单拒绝 + 关联打手 | `npx ts-node scripts/e2e-workbench.ts` |
| `e2e-chat.ts` | 下单→接单→发起会话→WS 双向实时→敏感词拦截→已读广播→未读数 | `npx ts-node scripts/e2e-chat.ts` |
| `e2e-admin.ts` | select 指定打手 + assign 派单→打手可见→接单 + 缺 boosterId 拦截 | `npx ts-node scripts/e2e-admin.ts` |
| `e2e-regression.ts` | 既有状态机回归：取消/支付/退款同意归零/退款驳回回退/接单完成计数 | `npx ts-node scripts/e2e-regression.ts` |

> 四个脚本全部通过（2026-08-18 实测 ✅）。脚本依赖 `wb_booster_1` 已绑定打手 1（夜风）、服务 1/2 存在、`config` 连接 6380 Redis。E2E 产生的脏数据（测试订单/会话）不影响功能。

### 冒烟（手测）
```bash
BASE=http://localhost:3000/api
curl -s -X POST $BASE/auth/wechat-login -H "Content-Type: application/json" -d '{"code":"customer1"}'  # 顾客 token
curl -s -X POST $BASE/auth/wechat-login -H "Content-Type: application/json" -d '{"code":"booster1"}'   # 打手 token（须已绑定）
```

## 8. 前端现状与待办（下一步）

### 小程序 `三角洲陪玩小程序/`
现状：10 页均为顾客端（首页/服务列表/服务详情/订单确认/订单支付/订单列表/订单详情/退款/我的/登录），原生 tabBar 三入口（首页/订单/我的）；`api/` 已对接真实后端（`config/index.js` `useMock=false`）。

**待开发（spec §9 有页面清单）**：
1. 顾客端对话页 `pages/chat/chat`（WS 订阅 + 历史分页 + 发送 + 敏感词预检）；会话列表页 `pages/chat-list/chat-list`（个人中心「消息」入口 + 未读角标，订单详情「联系打手」入口）。
2. 打手端：自定义底部导航组件 `components/booster-tabbar/`（工作台/订单/消息/我的）；工作台页（待接单池、接单、服务中、完成）；订单页；消息页；我的页（含绑定码状态）。
3. `api/` 新增：`booster.js`（bind/me/status/orders/accept/complete）、`chat.js`（conversations/messages/read）、sensitive 前端预检词库 `config/sensitive-words.js`。
4. 打手端导航用自定义组件（小程序 tabBar 只能一套，顾客/打手共用小程序，需按角色切换导航）。

### admin `admin/src/`
现状：6 个管理页，`api/request.js` 封装 token/解包/401 跳转。
**待开发**：
1. 敏感词管理页（`/admin/sensitive-words` CRUD）。
2. 订单页加「指派打手」操作（`POST /admin/orders/:id/assign`，弹层选 approved 打手）。
3. 打手页加「生成绑定码」按钮（展示 15 分钟有效的一次性码）。
4. 服务编辑/设置页加 `assignMode` 下拉（grab/assign/select）。

## 9. 已知坑与注意事项

1. **金额「分」、时间戳「毫秒」**——前端一律 `fenToYuan` / `new Date(ms)`。
2. **订单状态机两端必须一致**，新增 `pending_assign` 后：顾客端订单列表/详情要能展示"待接单"；`queryPayResult` 已把 `pending_assign` 计入已支付。
3. `WECHAT_MOCK` / `PAY_MOCK` **仅在显式 `=true` 时**生效，缺省视为真实（防漏配假支付）。
4. **接单需打手 `online=true && accepting=true`**（grab 模式）；BoosterGuard 要求打手 `audit==='approved'`。种子中只有打手 1「夜风」满足；`admin` 更新打手状态走 `PATCH /api/admin/boosters/:id`（audit 值 `approved/rejected`，**不是** `approve/reject`）。
5. 绑定闭环：admin 生成绑定码 → 用户 `POST /booster/bind` → 重新登录 role=booster。绑定后该微信用户即打手，不再能当普通顾客下单。
6. **敏感词词库当前通过 admin 接口手动注入**（22 个词，见 e2e 前注）。`scripts/seed.ts` 已写好幂等敏感词初始化，但**未重跑 seed**（seed 会清空全库）。如需干净数据：`cd xingjing-server && npm run seed`（生产环境拒跑；`ADMIN_PASSWORD` 缺省回退 admin123）。
7. **非 git 仓库**（`xingjing-server`、`admin`、小程序均无 `.git`），无提交历史；改动自行备份。
8. admin `api/request.js` 与 `stores/auth.js` **有意不互 import**（防循环），401 由 request 清 localStorage + `location.reload()`。
9. 服务器 / Docker 重启后需手动拉起（MySQL/Redis compose、后端、admin dev）。
10. 线上部署相关（腾讯云服务器、域名备案、微信认证/支付）见 `TODO.md`，目前处于等待期。

## 10. 关键文件索引

| 用途 | 路径 |
|---|---|
| 设计文档（含表结构/接口契约/前端页面清单） | `docs/superpowers/specs/2026-08-18-chat-booster-workbench-design.md` |
| 后端实施计划（8 任务，含全部代码片段） | `docs/superpowers/plans/2026-08-18-im-backend.md` |
| 前一轮联调交接 | `联调交接报告-2026-08-18.md` |
| 项目待办（服务器/域名/认证/上线） | `TODO.md` |
| 后端 E2E 脚本 | `xingjing-server/scripts/e2e-{workbench,chat,admin,regression}.ts` |
| 后端种子脚本 | `xingjing-server/scripts/seed.ts` |

---

**下一步建议**：优先实现小程序顾客端对话（会话列表 + 对话页 + 个人中心入口），再做打手端工作台（含自定义 tabbar 与角色分流），最后补 admin 的敏感词/派单/绑定码 UI。设计文档 §9 已给出完整页面与组件清单，可直接作为前端实施依据。
