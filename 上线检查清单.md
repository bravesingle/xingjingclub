# 星竞电竞 · 上线检查清单

> 生成日期：本次上线前收尾。资金一致性与安全硬伤已修复并实测通过，剩余为「上线前必配」与「需人工/商务确认」项。

---

## 一、本次已修复并验证（资金一致性 + 安全）

| # | 问题 | 修复 | 验证结果 |
| --- | --- | --- | --- |
| 1 | 退款不回滚分账 | 新增 `fundService.approveRefundSettlement`，事务原子化：扣回用户消费、作废打手收入（`incomes.status='reversed'`）、删除平台抽成 | ✅ 实测退款后 income 变 `reversed`、platform_income 清空 |
| 2 | 提现后改抽成破坏余额 | `recalcOrderIncome` 仅 `frozen` 可重算，且同步重算平台抽成 | ✅ 编译通过 + 逻辑校验 |
| 3 | 并发重复接单 | `acceptByBooster` 改原子 UPDATE（`WHERE booster_id IS NULL AND status='paid'`） | ✅ 编译通过 |
| 4 | 并发重复核销激活码 | `redeem` 改事务 + 悲观锁 | ✅ 实测同码二次兑换返回 400「已被使用」 |
| 5 | 并发超提 | `withdraw` 改事务 + 悲观锁锁打手行 | ✅ 实测超额提现返回 400「余额不足」 |
| 6 | 余额支付并发超扣/充值丢更新 | `deductBalance` 原子条件扣减、`recharge`/`addBalance` 原子累加 | ✅ 编译通过 |
| 7 | `queryPayResult` 误报 paid | `cancelled`/`pending_pay` 返回 `paid=false`；`paid/in_progress/completed/refunding/refunded` 返回 `true` | ✅ 实测 cancelled 返回 `paid=false` |
| 8 | 订单号/激活码可预测 | `genOrderNo`/`genCode` 改用 `crypto.randomInt` | ✅ 实测激活码为随机 12 位 |
| 9 | JWT_SECRET 弱默认 | 未配置直接启动报错；`NODE_ENV=production` 时强制 ≥32 位且非弱默认 | ✅ 逻辑校验 |
| 10 | CORS 全放开 | 改为白名单（`CORS_ORIGINS`，默认仅 localhost:5173） | ✅ 逻辑校验 |
| 11 | `synchronize` 生产风险 | 改为 `DB_SYNCHRONIZE` 开关，生产设 `false` | ✅ 逻辑校验 |

---

## 二、上线前必配（生产 `.env`）

生产环境 `.env` 必须逐项检查：

```env
NODE_ENV=production
DB_SYNCHRONIZE=false            # 生产禁用自动建表，改用 migration
DB_PASSWORD=<强随机密码>
REDIS_PASSWORD=<强随机密码>
JWT_SECRET=<至少32位强随机值>    # 生产强制校验，弱值直接启动失败
JWT_EXPIRES_IN=7d
CORS_ORIGINS=<管理后台生产域名>  # 逗号分隔，如 https://admin.example.com
WECHAT_APPID=<小程序appid>
WECHAT_SECRET=<小程序secret>
WECHAT_MOCK=false               # 接好真实登录后关闭
PAY_MOCK=false                  # 接好微信支付后关闭（否则充值/押金接口被禁用）
PAY_EXPIRE_MINUTES=15
```

> ⚠️ 强随机值生成示例（服务器上执行）：
> `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`

---

## 三、微信支付真实接入（当前唯一阻塞项）

现状：`PAY_MOCK=true`，支付/充值/押金全部走本地模拟，**不能用于真实收款**。已做防护：`PAY_MOCK=false` 时 `recharge`、`payDeposit` 直接抛错，杜绝免费入账。

上线真实收款前必须完成（`server/src/modules/pay/pay.service.ts` 的 `createPayment` 已预留接入点）：

1. 申请微信支付商户号（mchid）、APIv3 密钥、商户证书。
2. 实现 JSAPI 下单（`POST /v3/pay/transactions/jsapi`）+ 回调验签。
3. 支付回调（notify_url）里把订单 `pending_pay → paid`，并幂等处理（防重复回调）。
4. 微信登录 `jscode2session` 替换 mock（`WECHAT_MOCK=false` + 真实 appid/secret）。
5. 小程序端 `config/index.js` 的 `useMockPay` 同步切 `false`。

---

## 四、仍需人工确认/未实现项（评审遗留，不阻塞但有风险）

- **数据库迁移**：当前依赖 `synchronize`，生产设 `DB_SYNCHRONIZE=false` 前需先产出 migration，否则新字段/新表不会自动创建。
- **退款已提现场景**：若打手收入已解冻且存在「已打款」提现，系统会拒绝退款（`该订单打手收入已进入可提现且存在已打款记录`），需线下追回后人工处理。
- **敏感词过滤**：聊天内容未过滤，需接内容安全（如微信内容安全 API）或自建词库。
- **会话/未读数**：聊天有 WebSocket 实时，但无会话列表、未读消息数、消息持久化分页（当前仅按订单查历史）。
- **交接文档中的规划项**：`pending_assign` 派单状态、接单模式（抢单/派单）、打手绑定码、多打手协作等仍未实现。
- **日志/监控/备份**：无结构化日志、错误告警、数据库定时备份、PM2/Docker 进程守护（当前为裸 `node dist/main.js`）。
- **HTTPS**：小程序正式环境要求接口为 HTTPS + 备案域名，`config/index.js` 的 `baseUrl`/`wsUrl` 需改为线上域名（`ws://` 改 `wss://`）。

---

## 五、部署步骤（后端）

```bash
cd server
npm install
npm run build                 # 产出 dist/
# 按上面配置好生产 .env
node dist/main.js             # 或 pm2 start dist/main.js --name xjes
```

验证：
- `GET http://<域名>/api/home/data` 返回 `{code:0,...}`
- Swagger `http://<域名>/api/docs`

---

## 六、测试账号（seed 生成）

| 角色 | 账号 | 密码/手机 |
| --- | --- | --- |
| 超级管理员 | `admin` | `admin123` |
| 运营管理员 | `operator` | `operator123` |
| 已审核打手 | — | `13800000001`（夜风） |
| 普通玩家 | — | `13900000001`（阿凯） |

> 生产环境务必删除/重置这些 seed 账号密码，或不要在生产库执行 `npm run seed`（会清空全部数据）。
